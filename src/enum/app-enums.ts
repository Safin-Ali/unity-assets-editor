enum ENV {
    DEV = "0",
    PRO = "1",
}

enum Prompt {
    IncreaseSkinSlots = "iss",
    TrafficSpawn = "tsp"
}

export enum Endianess {
    L = "little",
    B = "big",
}

export const UABE_BUSSID = {
    ENV,
    Prompt,
    Endianess
};
